/* Newsletter validation and confirmation are owned by the embedded Brevo form.
 * Never treat iframe load as a successful subscription. Kept as a harmless
 * compatibility file for older WordPress script references. */
